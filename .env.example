# NBalance — Configuration
# Copier ce fichier en .env et adapter les valeurs

# PostgreSQL
POSTGRES_DB=nbalance
POSTGRES_USER=nbalance
POSTGRES_PASSWORD=nbalance_dev

DATABASE_URL=postgresql+asyncpg://nbalance:nbalance_dev@coulmes.cirad.fr:5432/nbalance
DATABASE_URL_SYNC=postgresql://nbalance:nbalance_dev@coulmes.cirad.fr:5432/nbalance

APP_NAME: str = "NBalance 1Max"
APP_VERSION: str = "0.1.1"



# Ports
DB_PORT=5432
APP_PORT=8765
FRONT_PORT=80

# Tag des images Docker (géré automatiquement par le CI)
TAG=latest
